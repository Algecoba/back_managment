# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`back_managment` is a backend management project. The repository is in early initialization — no framework, dependencies, or application code have been committed yet.

## Getting Started

The project has no build system or dependencies configured yet. As the stack is established, update this file with:
- Commands to install dependencies, build, lint, and run tests
- How to run a single test
- Environment variable setup (e.g., `.env.example`)
- Database migration instructions
